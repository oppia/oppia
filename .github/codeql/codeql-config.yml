name: "CodeQL Analysis File Ignoring Config"

paths-ignore:
  - core/tests/data
  # TODO(#16641): These files cause CodeQL to throw the error "Clear text
  # storage of sensitive information." Remove the following two lines once we
  # store the API key in a secrets manager on production.
  - core/controllers/acl_decorators.py
  - scripts/release_scripts/update_configs.py
