{% extends "base.html" %}

{% block content %}
  <div class="content-body">
    <div class="single-col">
      <h3>User Profile for {{user}}</h3>
      <br>
      {% for metric_data in metrics_data %}
        Course: <a href="/reader/{{metric_data.story_id}}">{{metric_data.story_name}}</a>
        <br>
        Metrics:
          {% if not metric_data.metrics %}
            None
          {% endif %}
          {% for metric in metric_data.metrics %}
            {{metric.key}}: {{metric.value}}
            <br>
          {% endfor %}
        <br>
      {% endfor %}
    </div>
  </div>
{% endblock %}
